{
  // Duplicate of the repo-root .vscode/settings.json: VS Code applies workspace
  // settings from the folder actually opened, and ScottDomains/ is opened as its
  // own folder (its lakefile.toml requires Mathlib; the repo root's does not).
  "lean4.input.languages": ["lean4", "lean", "markdown"]
}
