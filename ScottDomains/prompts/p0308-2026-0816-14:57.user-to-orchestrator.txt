---
prompt: p0308
type: interaction
opened: 2026-0816-14:57
closed: 2026-0816-15:01
subject: do-full
session: b8f2003b
---
# Reconstructed from Claude Code's session recording
# (/Users/scott/.claude/projects/-Users-scott-projects-ScottLean4/b8f2003b….jsonl),
# not from context: this conversation was compacted, so the assistant's
# own recollection covers only its tail. Timestamps are the recorded
# per-message times in America/Los_Angeles, second resolution.

## turn — user 2026-08-16T14:57:39.658000-07:00

do full

## turn — assistant 2026-08-16T14:58:23.211000-07:00

Fullness now reads off the transport. Adding it to `Restriction.lean`:

    Edit /Users/scott/projects/ScottLean4/ScottDomains/ScottDomains/EquilogicalSpaces/Restriction.lean
    Edit /Users/scott/projects/ScottLean4/ScottDomains/ScottDomains/EquilogicalSpaces/Restriction.lean
    Bash lake -d /Users/scott/projects/ScottLean4/ScottDomains build ScottDomains.EquilogicalSpaces.Restriction
## turn — assistant 2026-08-16T14:59:06.222000-07:00

Same reducibility issue — `restrictFunctor.obj B` doesn't unfold to `B.restrict` for the coercion. Retyping the representative first:

    Edit /Users/scott/projects/ScottLean4/ScottDomains/ScottDomains/EquilogicalSpaces/Restriction.lean
    Bash lake -d /Users/scott/projects/ScottLean4/ScottDomains build ScottDomains.EquilogicalSpaces.Restriction
## turn — assistant 2026-08-16T15:00:04.497000-07:00

The `have` erased the body, making `e'` opaque — it needs to be a `let` so the definitional equality survives:

    Edit /Users/scott/projects/ScottLean4/ScottDomains/ScottDomains/EquilogicalSpaces/Restriction.lean
    Bash lake -d /Users/scott/projects/ScottLean4/ScottDomains build ScottDomains.EquilogicalSpaces.Restriction
## turn — assistant 2026-08-16T15:00:23.069000-07:00

**`R` is full** — proved. Full build and land:

    Bash lake -d /Users/scott/projects/ScottLean4/ScottDomains build
    Bash wc -l /Users/scott/projects/ScottLean4/ScottDomains/ScottDomains/EquilogicalSpaces/Restriction.lean
    Edit /Users/scott/projects/ScottLean4/ScottDomains/ScottDomains/EquilogicalSpaces/README.md
    Edit /Users/scott/projects/ScottLean4/ScottDomains/ScottDomains/EquilogicalSpaces/README.md
    Edit /Users/scott/projects/ScottLean4/ScottDomains/ScottDomains/EquilogicalSpaces/README.md
    Bash /Users/scott/projects/ScottLean4/scripts/gitcp.sh "r0071: R is full  Restriction.lean 114 -> 155 lines, still 0 sorry. Three of Theorem 3.12's four pi
## turn — assistant 2026-08-16T15:01:27.868000-07:00

**`R` is full** — pushed as **a9cf0a2**. Three of Theorem 3.12's four pieces are done.

## The proof

Fullness reads off r0070's `extension_into_algebraicLattice`, exactly as the paper says it should: pick a representative `e` on the total parts, regard it as a continuous map into `|B|` by composing with the inclusion, extend to all of `|A|`.

**Equivariance of the extension is free**, and that's the point: `A.Rel x y` already forces `x` and `y` total, so at exactly those points the extension agrees with `e`, and `e` is equivariant. Nothing needs to be known about the extension off the total part — the same observation that made *faithfulness* hold in r0067, now used in the opposite direction.

The Scott topology on `𝒫(K |B|)` isn't ambient — no `PEqu` object carries one — so it's introduced locally with `letI`, and `IsScott` then holds by `rfl`.

## Two Lean details, each one build iteration

- `restrictFunctor.obj B` doesn't unfold to `B.restrict` for unification, so the representative must be retyped before its `Subtype` projection and `equivariant` field will elaborate.
- **That retyping must be `let`, not `have`.** `have` erases the body, making the retyped term an opaque local, and the final goal needs it definitionally equal to the original. My first attempt used `have` and failed with *"show tactic failed, pattern not definitionally equal to target"* — a clean illustration of why `have` and `let` aren't interchangeable for data.

## Correction

Backticks in the commit message were executed by zsh again, mangling four spots in a9cf0a2's body (the `let`/`have` discussion). I flagged this after ac083ec and said I'd avoid backticks — I didn't. The technical content is damaged in that message; the code and this summary are correct.

## State

| Module | Lines | `sorry` |
| --- | ---: | ---: |
| `Restriction.lean` | 155 | **0** |
| `PowersetRetract.lean` | 167 | **0** |
| `PartialEquilogical.lean` | 286 | 1 |
| `Theorems3.lean` | 161 | 4 |
| *(8 others)* | 1287 | **0** |
| **Total** | **2056** | **5** |

1548 jobs, 0 errors.

**3.12 needs one more piece: essential surjectivity.** The witness is `𝒫 Ω_ℰ` carrying the relation transported along `nbhdFilter`, whose total part is `range nbhdFilter`, homeomorphic to `ℰ` by Theorem 3.6. Every prerequisite is in hand — including `IsAlgebraic (Set X)` from `ScottDomains.Powerset`. Then `Equivalence.ofFullyFaithfullyEssSurj` closes 3.12.

