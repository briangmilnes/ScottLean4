---
prompt: p0044
type: interaction
opened: 2026-0806-17:39
closed: 2026-0806-17:44
subject: why-cant-you-run-them-in-parallel-yourse
session: a2827760
---
# Reconstructed from Claude Code's session recording
# (~/.claude/projects/-home-milnes-projects-ScottLean4/a2827760….jsonl),
# not from context: this conversation was compacted, so the assistant's
# own recollection covers only its tail. Timestamps are the recorded
# per-message times in America/Los_Angeles, second resolution.

## turn — user 2026-08-06T17:39:34-07:00

Why can't you run them in parallel yourself? In different trees? You just tell each one which tree. I've done it before.

## turn — assistant 2026-08-06T17:39:56-07:00

You're right, and I was wrong — I can launch them with the Agent tool and tell each which tree. Let me correct one real blocker first, though: all 10 statements are in **one file**, so three agents editing `Skeleton.lean` would collide on merge. Splitting it into three files, one owned per agent:

    Write /home/milnes/projects/ScottLean4/ScottDomains/ScottDomains/Skeleton/Lemma10.lean
    Write /home/milnes/projects/ScottLean4/ScottDomains/ScottDomains/Skeleton/Section6.lean
    Write /home/milnes/projects/ScottLean4/ScottDomains/ScottDomains/Skeleton/Lemma17.lean
    Bash cd /home/milnes/projects/ScottLean4/ScottDomains; rm ScottDomains/Skeleton.lean; lake build 2>&1 | grep -A5 "error" | head -20; echo "=== result ===";
    Bash cd /home/milnes/projects/ScottLean4; scripts/gitcp.sh "r0026: split the skeleton into three files, one per agent, so parallel work cannot collide on a
    Bash cd /home/milnes/projects/ScottLean4; git status --short | head; echo "--- staging the deletion too ---"; git add -A ScottDomains/ScottDomains; git sta
    Bash cd /home/milnes/projects/ScottLean4; scripts/gitcp.sh "r0026: remove the monolithic Skeleton.lean, superseded by the three per-agent files" 2>&1 | tai
    Bash cd /home/milnes/projects/ScottLean4-agent1; git merge main -m "sync to main" 2>&1 | tail -1; cd /home/milnes/projects/ScottLean4-agent2; git merge mai
## turn — assistant 2026-08-06T17:42:08-07:00

All three worktrees synced. Launching the agents:

    Agent Prove Lemma 10 in agent1 tree
    Bash cd /home/milnes/projects/ScottLean4; ls -la .claude/; echo "--- local settings content ---"; cat .claude/settings.local.json 2>/dev/null || echo "(no 
## turn — assistant 2026-08-06T17:43:01-07:00

Exactly right — that's the gap. The worktrees have the **tracked** `.claude/settings.json` (merged in) but not `.claude/settings.local.json`, which is untracked and therefore absent from every worktree. It carries `env.TZ` and the broad `Read(//home/milnes/**)` grant the agents need.

    Bash cp /home/milnes/projects/ScottLean4/.claude/settings.local.json /home/milnes/projects/ScottLean4-agent1/.claude/settings.local.json; cp /home/milnes/p
    Agent Prove §6 results in agent2 tree
    Agent Prove Lemma 17 in agent3 tree
    Edit /home/milnes/projects/ScottLean4/.claude/settings.local.json
    Bash cp /home/milnes/projects/ScottLean4/.claude/settings.local.json /home/milnes/projects/ScottLean4-agent1/.claude/settings.local.json; cp /home/milnes/p
## turn — assistant 2026-08-06T17:44:54-07:00

I've now failed twice to fix this blind, so let me stop guessing and give you the diagnosis plus a decision.

**What I think is happening:** Claude Code reads `permissions.additionalDirectories` at **session start**. I added it mid-session, so it almost certainly isn't active for this session — which means every Edit/Write the three agents attempt inside `~/projects/ScottLean4-agent{1,2,3}` is treated as a write outside the project root and prompts, no matter what I put in the settings files now. Adding allow-rules doesn't help either, because the directory check runs before the command check.

I've written the entries into both `settings.json` and `settings.local.json` and copied them into all three worktrees, so they'll be correct for the *next* session. They probably won't help this one.

Three ways forward — your call:

1. **Restart the session.** The settings are already on disk and correct; the agents' branches (`agent1`/`agent2`/`agent3`) and the worktrees persist. Relaunching the three agents after restart should run clean. Costs a restart, keeps the parallelism.
2. **Click "always allow" once** on the next prompt for each of Edit/Write/Bash in a worktree. That persists the rule and the remaining prompts stop. Costs three clicks, no restart.
3. **Kill the agents and I work the ten statements sequentially** in the main tree. Zero prompts, because nothing writes outside the project root — but no parallelism, and it discards the setup we just built.

My recommendation is **2** if you're at the keyboard, **1** if you want to walk away — because option 3 throws away infrastructure that measured out well (three worktrees, 970 MiB total, all building 962 jobs, shared Mathlib confirmed unwritten).

Worth saying plainly: the disk analysis and the worktree mechanics were right, and the split-by-file design means the agents genuinely cannot collide. The thing I got wrong was assuming a mid-session settings edit would take effect, and I should have checked that before launching three agents rather than after.

The agents are still running in the background. If you pick 1 or 3 I'll stop them first; if you pick 2 they'll carry on from where they stalled.

